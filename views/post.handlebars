<div class="text-center">
  <h2>{{username}}</h2>
  <h3>{{title}}</h3>
  <p>{{content}}</p>
  <p>Created by {{username}} on {{format_date date_created}}</p>
  <div class="col-md-12">
        <button class="btn btn-sm btn-danger" updateData-id="{{posts.id}}">UPDATE</button>
      </div>
</div>

